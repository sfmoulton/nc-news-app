import React from "react";
import { Link } from "@reach/router";

const LogInButton = () => {
  return <Link to="/log_in">
    <button>
      Log In
    </button>
  </Link>;
};

export default LogInButton;
